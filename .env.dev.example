# Vite development environment variables
# Port of the backend server that Vite will proxy API/WebSocket requests to.
# This is NOT the port Vite listens on (Vite runs on port 3000).
VITE_BACKEND_PORT=3087
