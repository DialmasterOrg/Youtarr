'use strict';

 // This filename belongs to the pre-normalization foundation draft. The
 // executable migration was reissued after upstream's schema normalization;
 // retaining this identity keeps already-recorded and partially applied
 // installations from replaying stale mixed-case DDL.
 module.exports = {
   async up() {},
   async down() {},
 };
