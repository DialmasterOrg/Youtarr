// shared.ts — common non-MUI theme utilities
// Previously contained MUI ThemeOptions / component overrides.
// All styling is now handled via CSS variables and Tailwind.

/** @deprecated MUI removed — kept for import safety until fully cleaned. */
export const fabBase = {};

/** @deprecated MUI removed — kept for import safety until fully cleaned. */
export const commonThemeOptions = {};
